---
import type { Product } from "../data/products";

interface Props {
  product: Product;
}

const { product } = Astro.props as Props;
---

<article class="relative rounded-xl border border-stone-200 bg-white p-4 shadow-sm">
  <span class="absolute left-4 top-4 rounded-full bg-emerald-100/90 px-3 py-1 text-[11px] font-extrabold tracking-wide text-emerald-800">Available</span>
  <a href={`/products/${product.slug}`} class="block">
    <img class="mb-4 mt-8 h-64 w-full rounded-lg border border-stone-200 bg-white object-contain p-2" src="/productmockup.jpg" alt={`${product.name} pickle mockup`} loading="lazy" />
  </a>
  <div class="flex items-start justify-between gap-3">
    <div>
      <a href={`/products/${product.slug}`} class="text-lg font-bold text-slate-900 hover:text-brand-600">{product.name}</a>
      <p class="text-sm font-semibold text-slate-500">{product.malayalam}</p>
      <p class="mt-1 text-xs text-slate-500">{product.tagline}</p>
    </div>
    <span class="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-700">₹{product.price} | {product.unit}</span>
  </div>
  <p class="mt-2 text-sm text-slate-600">{product.shortDescription}</p>

  <div class="mt-4 flex gap-2">
    <a href={`/products/${product.slug}`} class="inline-flex items-center justify-center rounded-lg border border-stone-300 bg-white px-3 py-2 text-xs font-bold text-slate-700 hover:bg-stone-100">View Details</a>
    <button
      class="inline-flex items-center justify-center gap-2 rounded-lg bg-slate-900 px-3 py-2 text-xs font-bold text-white hover:bg-slate-800"
      data-add-to-cart
      data-product-slug={product.slug}
      data-product-name={product.name}
      data-product-price={product.price}
      data-product-image="/productmockup.jpg"
    >
      <i class="fa-solid fa-cart-plus"></i>
      Add to Cart
    </button>
  </div>
</article>
